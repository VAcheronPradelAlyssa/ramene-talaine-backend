spring:
  application:
    name: ramene-talaine-backend

  datasource:
    url: jdbc:postgresql://localhost:5432/ramene_talaine
    username: ramene_admin
    password: ChangeMe123!
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

management:
  endpoints:
    web:
      exposure:
        include: health, info
  endpoint:
    health:
      show-details: always